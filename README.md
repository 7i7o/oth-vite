# Ohtent + Vite

Testing a simple app with Othent using Vite

## Install

Clone the repo, then install deps:

`npm install`

or

`yarn install`

## Run locally

To run the server:

`npm dev`

or

`yarn dev`
